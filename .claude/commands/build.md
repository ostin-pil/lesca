# Build Project

Build the Lesca project for production.

## Steps

1. Clean previous build: `npm run clean`
2. Run build: `npm run build`
3. Report any build errors with file:line references

If build fails, analyze the errors and provide specific fixes.
